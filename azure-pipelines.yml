# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- netstandard

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  version: '1.0.0'
  major: '1'
  minor: '0'
  patch: '0'

steps:
- script: dotnet build --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'
#- script: dotnet publish 'qif/qif.csproj' --output $(Build.ArtifactStagingDirectory)
#  displayName: 'dotnet publish'
- script: dotnet pack 'qif/qif.csproj' /p:PackageVersion=1.0.1.$(Build.BuildNumber) --output $(Build.ArtifactStagingDirectory) --no-build
  displayName: 'dotnet pack'
- task: NuGetCommand@2
  displayName: 'Push nuget package to DevOps feed'
  inputs:
    command: push
    publishVstsFeed: 'Lien'
    # nuGetFeedType: external
    # publishFeedCredentials: ''
    # versioningScheme: byEnvVar
    # versionEnvVar: version
    # versioningScheme: byPrereleaseNumber
    # majorVersion: '$(major)'
    # minorVersion: '$(minor)'
    # patchVersion: '$(patch)'
